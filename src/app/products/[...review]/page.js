

const page = ({params}) => {
    console.log(params);
    return (
        <div>
            <h2 className="text-lg font-bold">Hello This is Review Page</h2>
        </div>
    );
};

export default page;